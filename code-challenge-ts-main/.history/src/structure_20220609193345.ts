
export const structure = {
    heading = (customer : any) => `Rental Record for ${customer}`,
    movieList = (movies : any, moviePrices : any) => [
        `${movies[0]} - 3.5
        The Two Towers - 15
        The Return of the King - 3`],
    balance = `Amount owed is 21.5`,
    rewards = `You earned 4 frequent renter points`

}